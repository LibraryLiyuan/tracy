interface:
  display_name: "JN Unity × Tracy 性能诊断"
  short_description: "用 Query 原生全量扫描完成单 Trace 深度诊断"
  default_prompt: "使用 $diagnose-jn-unity-tracy 对一个现有 JN Unity Tracy 捕获执行 Query 1.35 确定性全量扫描，逐项调查 P0/P1/UserFocus 候选，并生成包含完整分析过程的可审计报告。"

policy:
  allow_implicit_invocation: true
